apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-todo-add-task-ingress
spec:
  ingressClassName: {{.Values.common.ingress.className}}
  rules:
  - host: {{.Values.Addtask.ingress.host}}
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: {{ .Release.Name }}-todo-add-task-service
            port:
              number: {{ .Values.Addtask.port }}
