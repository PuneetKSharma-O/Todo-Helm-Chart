apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-todo-get-task-ingress
spec:
  ingressClassName: {{.Values.common.ingress.className}}
  rules:
  - host: {{.Values.Gettask.ingress.host}}
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: {{ .Release.Name }}-todo-get-task-service
            port:
              number: {{ .Values.Addtask.port }}
