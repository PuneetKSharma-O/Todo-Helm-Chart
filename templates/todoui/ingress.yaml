apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-todo-ui-ingress
spec:
  ingressClassName: {{.Values.common.ingress.className}}
  rules:
  - host: {{.Values.todoui.ingress.host}}
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: {{ .Release.Name }}-todo-ui-service
            port:
              number: {{ .Values.todoui.port }}
